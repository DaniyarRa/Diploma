version: '3.8'

services:
  web:
    build: .
    volumes:
      - .:/code
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
    command: gunicorn --bind :$PORT DiplomaProject_1.wsgi:application

